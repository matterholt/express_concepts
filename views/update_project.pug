extends layout

block content
    h1=title


    ul
        li=data.project_id
        li=data.code
        li=data.part

    h3 Remove project
    
    form(method="POST" action='/'+data.project_id+'/delete/')
      div
        input#projectid(type='hidden',name=data.project_id, required='true',value=data.project_id)
        
      button(type='submit') Delete
    
    p.note future should have more info, such as import dates and users on project
